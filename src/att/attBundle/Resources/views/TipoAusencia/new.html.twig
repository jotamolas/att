{% extends 'attBundle::base.html.twig' %}

{% block body %}
    <h1>Registrar Tipo de Ausencia</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('att_tipoaus_index') }}">Volver</a>
        </li>
    </ul>
{% endblock %}
